import 'package:flutter/material.dart';

class FavoriteProvider extends StatefulWidget {
  const FavoriteProvider({super.key});

  @override
  State<FavoriteProvider> createState() => _FavoriteProviderState();
}

class _FavoriteProviderState extends State<FavoriteProvider> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: SingleChildScrollView(
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}